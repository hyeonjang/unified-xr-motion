\documentclass[runningheads]{llncs}

\usepackage[T1]{fontenc}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{array}
\usepackage{xcolor}
\usepackage{tikz}
\usepackage{url}
\usepackage{listings}
\graphicspath{{figures/}{figures/latency/}{figures/latency/figures/}}
\usepackage{float}
\usetikzlibrary{arrows.meta,positioning,calc,shapes.geometric}
\urlstyle{rm}

\definecolor{uxblue}{RGB}{42,94,160}
\definecolor{uxgreen}{RGB}{58,130,83}
\definecolor{uxorange}{RGB}{186,104,41}
\definecolor{uxgray}{RGB}{235,235,235}
\newcolumntype{Y}{>{\raggedright\arraybackslash}X}
\newcolumntype{P}[1]{>{\raggedright\arraybackslash}p{#1}}
\lstdefinestyle{csharp}{basicstyle=\ttfamily\scriptsize,breaklines=true,frame=single,columns=fullflexible,numbers=none,backgroundcolor=\color{uxgray},rulecolor=\color{black!25}}

\begin{document}

\title{UnifiedXRMotion: A Validity-Aware Runtime Contract for Heterogeneous XR Avatar Motion}
\titlerunning{UnifiedXRMotion}
\author{Anonymous Authors}
\authorrunning{Anonymous Authors}
\institute{Paper under double-blind review}
\maketitle

\begin{abstract}
XR avatar-motion pipelines often combine sources that differ in body coverage, joint layout, coordinate convention, and component availability. A headset, controller pair, articulated hand tracker, vision model, replay stream, inferred-motion module, and retargeting node may all produce motion for the same avatar pipeline, but downstream consumers should not need source-specific logic for every case. We present UnifiedXRMotion, a Unity prototype organized around a validity-aware runtime contract for heterogeneous XR avatar motion. The contract uses a canonical skeletal schema, reusable sub-skeleton views, and per-joint, per-component availability bits. Observed, inferred, replayed, synthetic, and retargeted components flow through the same skeletal representation; provenance remains implicit in the producing node rather than encoded in the data record. SDK and vision adapters map native observations into the contract, while downstream retargeting, recording, and optional transport components consume shared views and gate reads on explicit availability. Evaluation combines implementation evidence, adapter/producer coverage, reusable-consumer evidence, and a within-subjects setup-burden study. The results support a scoped systems contribution: a concrete contract and boundary for composing selected heterogeneous XR avatar-motion sources without requiring downstream consumers to depend on SDK-specific records.
\keywords{Extended reality \and Avatar motion \and Tracking middleware \and Motion-data contract \and Runtime systems \and Authoring}
\end{abstract}

\section{Introduction}\label{sec:introduction}
XR avatars depend on motion pipelines that span tracking acquisition, skeletal representation, motion processing, retargeting, recording, optional transport, and rendering. In practice these stages are often tied to one SDK or one avatar asset. A hand tracker may expose a vendor-specific joint order; a body tracker may report only a subset of joints; a vision model may provide positions but not reliable rotations; a replay stream may preserve previously recorded availability; and an inference or retargeting node may publish components that were not directly observed. This heterogeneity is not merely inconvenient engineering. It makes it hard to replace trackers, compare pipelines, record and replay motion, or reuse downstream avatar consumers without rewriting glue code.

Existing XR APIs and tracking middleware address important parts of this problem. OpenXR standardizes access to XR devices and extensions such as hand tracking~\cite{khronos_openxr_1_0,openxr_ext_hand}. Earlier VR middleware, including VRPN~\cite{taylor2001vrpn}, OpenTracker~\cite{reitmayr2001openarchitecture}, and Ubiquitous Tracking~\cite{newman2004ubiquitous}, showed that device abstraction and distributed tracking are valuable architectural tools. UnifiedXRMotion builds on that lesson at a narrower layer: avatar-motion pipelines that must carry incomplete skeletal components through adapters, filters, inferred-motion producers, retargeters, record/replay utilities, and optional network wrappers.

The central design choice is to separate \emph{slot identity} from \emph{component availability}. A schema defines addressable motion slots and region views; a producer publishes the slots and components it has available; each pose component is guarded by a validity bit; and downstream consumers read only the components whose bits are set. This contract does not assert that a component is ground-truth correct, directly measured, or high-confidence. It only states that the producing node has made that component available for downstream consumption under its own semantics. In the current prototype, the contract is instantiated with a 77-slot full-body schema covering torso, limbs, head, and articulated hands. This layout is one implementation instance rather than a general skeleton standard.

This paper makes three contributions:
\begin{itemize}
    \item \textbf{A validity-aware skeletal motion contract} that combines stable slot identities, a canonical 77-slot prototype schema, sub-skeleton views, per-slot pose records, and per-component availability flags.
    \item \textbf{A heterogeneous producer and adapter boundary} that maps selected XR, vision, replay, synthetic, inferred-motion, and retargeted producers into the shared representation while localizing source-specific joint ordering, coordinate conversion, body coverage, and missing-component semantics.
    \item \textbf{Validity-gated downstream consumption and setup evidence}, demonstrated by retargeting, recording, and optional transport consumers that use shared contract views, plus a within-subjects Unity setup-burden study for the tested authoring task.
\end{itemize}

\section{Related Work}\label{sec:related}
\paragraph{Tracking middleware and XR APIs.}
VRPN provides a device-independent and network-transparent interface for VR peripherals~\cite{taylor2001vrpn}. OpenTracker introduced a modular software architecture for tracking configurations~\cite{reitmayr2001openarchitecture}, and Ubiquitous Tracking modeled distributed spatial relationships as a graph of tracking transformations~\cite{newman2004ubiquitous}. OpenXR standardizes XR device access and extensions such as hand tracking~\cite{khronos_openxr_1_0,openxr_ext_hand}; platform SDKs add body, hand, controller, camera, or device-specific streams~\cite{meta_and_openxr_2025,vive_openxr_2024,xreal_sdk_3_0_2024,android_xr_sdk_2024}. These systems abstract devices, transforms, or API access. UnifiedXRMotion instead targets a skeletal avatar-motion record in which individual pose components may be unavailable and consumers explicitly gate reads using component availability.

\paragraph{Avatar motion, retargeting, and inference.}
Sparse avatar-motion reconstruction and motion priors have advanced rapidly~\cite{jiang2022avatarposer,dai2024hmd,xu2024mobileposer,yang2024divatrack,starke2024categorical,ProtoMotions}. Avatar representation, fidelity, and retargeting affect embodiment and agency~\cite{yun2023animationfidelity,cheymol2023beyond,weidner2023avatarsagents}, and AvatarGo studies plug-and-play self-avatar setup~\cite{ponton2022avatargo}. UnifiedXRMotion does not introduce a new tracker, IK solver, retargeting algorithm, or codebook method. Instead, textbook IK~\cite{aristidou2011fabrik}, codebook-style inference inspired by prior work~\cite{starke2024categorical}, and retargeting components are treated as representative nodes that consume and republish the same motion contract.

\section{Validity-Aware Motion Contract}\label{sec:contract}
UnifiedXRMotion treats skeletal motion as a runtime contract between motion producers and consumers. The contract consists of a schema, region views, per-slot pose records, and per-component availability bits. A slot is a stable address in the motion representation, such as a head, wrist, fingertip, tracker, prop, facial control, or application-specific joint. Each slot may carry position, rotation, velocity, and angular velocity components; each component has an explicit bit indicating whether it is available in the current record.

A contract instance separates static schema from runtime state. The static schema is
\begin{equation}
    \mathcal{S}=(S,P,R,K),
\end{equation}
where $S=\{s_0,\ldots,s_{n-1}\}$ is a finite set of motion slots, $P$ is an optional parent relation, $R$ is a set of named region views with each $r_j\subseteq S$, and $K$ is the set of pose-component kinds. A runtime sample stores
\begin{equation}
    M=\{(m_i,V_i)\}_{i=0}^{n-1}, \qquad
    m_i=(p_i,q_i,v_i,\omega_i), \qquad
    V_i=(b^p_i,b^q_i,b^v_i,b^\omega_i),
\end{equation}
with $b_i^k\in\{0,1\}$. A consumer may read component $m_i^k$ only when $b_i^k=1$. The prototype implements this rule through \texttt{PhysicBone}, which stores pose fields and a \texttt{PoseFlag} bitmask, and through \texttt{KinematicBuffer<T>} and \texttt{BodyType} views over full-body, upper-body, lower-body, two-arm, two-hand, and hand regions.

\begin{figure}[t]
\centering
\includegraphics[width=0.94\textwidth]{fig2-kinematicbuffer.pdf}
\caption{Prototype buffer and sub-skeleton-view instantiation of the contract. A canonical full-body buffer is exposed through region views so that producers and consumers can operate on the subset they require.}
\label{fig:contract}
\end{figure}

\begin{table}[t]
\caption{Contract concepts and evidence scope. Validity denotes component availability, not correctness, confidence, or provenance.}\label{tab:contract}
\scriptsize
\begin{tabularx}{\textwidth}{P{25mm}YY}
\toprule
Concept & Prototype evidence & Scope boundary \\
\midrule
Schema and slots & 77-slot full-body prototype schema with parent maps and sub-skeleton views. & One schema instantiation; arbitrary rigs require schema agreement and retargeting. \\
Pose record & Per-slot \texttt{PhysicBone} fields for position, rotation, velocity, scalar angular velocity, and local transforms. & The record does not carry timestamp, continuous confidence, or source provenance. \\
Availability & \texttt{PoseFlag} marks position, rotation, velocity, and angular-velocity availability per slot. & Availability does not imply ground-truth correctness or direct sensor observation. \\
Region views & \texttt{KinematicBuffer<T>} and \texttt{BodyType} expose full-body, upper/lower-body, arms, hands, and hand views. & Views are implementation-defined rather than discovered from arbitrary avatars. \\
Serialization & Optional transport writes the flag first and can omit unavailable components. & Used as semantic preservation; this paper does not frame the contribution as compression. \\
\bottomrule
\end{tabularx}
\end{table}

This distinction is essential for inferred and retargeted motion. A lower-body inference node may publish a rotation as available, and a retargeting node may publish an avatar-specific component as available, but the contract does not encode whether that component was observed, inferred, reconstructed, replayed, or retargeted. Provenance is implicit in the producing node. Likewise, a vision adapter may threshold continuous confidence into a binary availability bit, but the continuous confidence value is not preserved in the current record. These are deliberate scope boundaries for the present prototype.

\section{Heterogeneous Producers and Adapter Boundary}\label{sec:adapters}
Producers publish motion into the contract. Device and SDK adapters are the boundary where native tracking outputs become contract records. Each adapter initializes or reads a source, maps native joint ordering into schema slots, converts coordinate conventions where needed, marks component availability, and publishes pose records. The adapter code remains vendor-specific wrapper and conversion logic; the reusable artifact is the boundary after publication, where downstream nodes depend on slot identity and component availability rather than native SDK records.

The prototype exposes a minimal adapter interface through \texttt{ITrackingDeviceHandler}: initialization, a set of tracked schema slots, a tracking-information buffer, and a tracking update. Its producer portfolio includes OVR/Meta body and hand streams, Unity XR/OpenXR HMD, controller, and hand sources, XREAL/NRSDK and Vive hand sources, Mediapipe vision sources, Android-AR or scene-transform sources, replay streams, synthetic sources, and representative inferred or retargeted producers. Table~\ref{tab:producers} summarizes the evidence used in the medium-paper framing.

\begin{table}[t]
\caption{Producer and adapter evidence. The table reports selected implementation evidence, not broad device coverage.}\label{tab:producers}
\scriptsize
\begin{tabularx}{\textwidth}{P{25mm}P{27mm}YY}
\toprule
Producer family & Source examples & Contract mapping & Important limitation \\
\midrule
Observed XR devices & Meta/OVR body and hand, Unity XR/OpenXR HMD/controllers/hands, Vive hands, XREAL hands. & Native joint IDs and device poses are mapped into full-body or region slots with per-component availability. & Adapter logic is SDK-specific; coverage is limited to selected source families. \\
Vision sources & Mediapipe upper-body and hand landmarks. & Landmark positions are mapped into hand or upper-body slots; visibility is thresholded into binary availability. & Continuous confidence is quantized away; coordinate offsets remain adapter assumptions. \\
Replay and synthetic sources & Recorded \texttt{PhysicBone} arrays, transform sources, dummy or mouse-driven inputs. & Publish the same slot records and flags as live sources. & Fidelity depends on the recorded stream or synthetic setup. \\
Inferred producers & Codebook-style lower/upper-body regressors and filters. & Publish inferred components back into \texttt{KinematicBuffer<PhysicBone>} with availability flags. & Inference algorithm and model quality are not contributions; provenance is not encoded in the record. \\
Retargeted producers & IK and retargeting nodes that fill avatar-motion regions. & Consume views and may republish retargeted components into the same representation. & IK and retargeting methods are representative nodes, not algorithmic novelty. \\
\bottomrule
\end{tabularx}
\end{table}

The adapter boundary localizes several classes of heterogeneity: vendor joint ordering, handedness and axis conversion, body-region coverage, missing velocity or angular-velocity channels, and SDK tracking-state or visibility conversion. The boundary is not leak-free. For example, vision visibility is reduced to a binary bit, and some device adapters contain source-specific offsets or calibration assumptions. These limitations are why the claim is limited to selected producer families and explicit component availability, not to broad cross-device coverage or automatic calibration.

\section{Validity-Aware Consumers}\label{sec:consumers}
The strongest consumer evidence is the retargeting path. \texttt{RetargetSystem} reads \texttt{KinematicBufferView<PhysicBone>} instances and checks \texttt{PoseFlag} before assigning position or rotation to avatar transforms. If a component is unavailable, the assignment is skipped rather than silently using a default or stale source value. The same consumer dispatches across \texttt{BodyType} views such as full body, upper/lower body, two arms, and two hands, so the consuming code is organized around contract views rather than around a producing SDK.

\begin{figure}[t]
\centering
\includegraphics[width=0.88\textwidth]{fig3-retarget.pdf}
\caption{Representative retargeting consumers reading contract views and writing to avatar rigs. Retargeting is used as evidence of validity-gated consumption, not as an algorithmic contribution.}
\label{fig:retarget-consumer}
\end{figure}

\begin{table}[t]
\caption{Downstream consumers and evidence scope.}\label{tab:consumers}
\scriptsize
\begin{tabularx}{\textwidth}{P{27mm}P{32mm}YY}
\toprule
Consumer & Contract use & Supported claim & Unsupported claim \\
\midrule
Retargeting system & Reads region views and gates transform assignment on \texttt{PoseFlag}. & Same consumer structure can handle multiple sub-skeleton views. & New retargeting algorithm or avatar-quality guarantee. \\
Recording/replay & Stores and republishes \texttt{PhysicBone} arrays with flags. & Replay can act as another producer in the same representation. & Ground-truth motion or accuracy benchmark. \\
Network serialization & Writes \texttt{PoseFlag} before optional channels and preserves availability semantics. & Optional transport can carry partial records. & Compression result, runtime-speed result, or transparent equivalence. \\
Filters/inference nodes & Read upstream views and republish processed or inferred components. & Observed and inferred components flow through one representation. & Encoded-provenance or uncertainty processing. \\
\bottomrule
\end{tabularx}
\end{table}

This consumer evidence is deliberately narrower than claiming that every module is SDK-agnostic. Some concrete retargeters or adapters are vendor-specific by design. The medium-paper claim is that the common contract gives downstream consumers a shared availability-gated representation, and that representative retargeting, recording, inference, replay, and transport nodes operate on that representation without depending on each native SDK record format.

\section{Prototype Implementation}\label{sec:implementation}
UnifiedXRMotion is implemented as a Unity package. Motion slots are stored in preallocated buffers; region views expose subsets without requiring a separate representation for each body part. Root producers are advanced by a frame-driven trigger list, and motion updates propagate through sender/receiver events. This runtime organization is a Unity-oriented composition mechanism rather than a general graph scheduler or graph optimizer.

The package includes representative nodes for filtering, IK, retargeting, codebook-style inference, record/replay, and optional FishNet-based transport. Textbook IK and prior codebook-style control provide algorithmic background~\cite{aristidou2011fabrik,starke2024categorical}; the contribution here is that these nodes can consume or publish the same validity-aware representation. Optional transport preserves flags and available components, but transport is treated as implementation support rather than the center of the paper.

\section{Evaluation and Case Study Evidence}\label{sec:evaluation}
The evaluation is scoped to the medium-paper claim. It asks whether the contract is instantiated in a concrete runtime, whether selected heterogeneous producers can be localized at adapters while consumers reuse contract views, and whether the authoring workflow reduces procedural setup burden in one controlled Unity task. The evaluation does not measure tracking accuracy, retargeting quality, general developer productivity, broad device coverage, or display-response timing.

\paragraph{Contract, producers, and consumers.}
Tables~\ref{tab:contract}, \ref{tab:producers}, and~\ref{tab:consumers} summarize the implementation evidence. The contract evidence is the record/view/flag mechanism; the producer evidence is the selected adapter and producer portfolio; and the consumer evidence is validity-gated consumption through retargeting, recording, inference/replay, and optional transport. This evidence supports a concrete runtime contract and boundary, not a claim that the system solves all avatar-motion interoperability problems.

\paragraph{Setup-burden study.}
We conducted a within-subjects Unity setup task comparing a UnifiedXRMotion workflow against a vendor SDK workflow. Participants configured the same scene-level goal in both conditions: making custom OpenXR hand prefabs follow simulated hand motion and making the same Y Bot avatar follow simulated full-body motion. Both conditions used the Meta XR Simulator for verification. The study used Unity 6000.0.33f1 and Meta Core, Movement SDK, and XR Simulator version 78.0.0.

To avoid confounding pipeline assembly with mesh-specific hand-axis calibration, custom hand retargeting and axis alignment were completed before each session. Participants were instructed to prioritize correctness and completion rather than speed. We therefore interpret completion time as procedural setup burden for the tested task, not as a general developer-speed estimate. The procedure also excludes adapter authoring and full integration of a previously unseen hand mesh.

\begin{table}[t]
\caption{Within-subjects setup-burden results ($N=19$). Values are mean $\pm$ standard deviation. NASA--TLX is the 0--100 workload score after reversing the performance subscale so that higher values consistently indicate higher workload. Completion time is interpreted as procedural setup burden for the tested Unity task.}\label{tab:user-results}
\scriptsize
\centering
\begin{tabularx}{\textwidth}{P{27mm}rrr}
\toprule
Statistic & Completion time (s) & NASA--TLX & SUS \\
\midrule
UnifiedXRMotion ($M\!\pm\!SD$) & $564.03\!\pm\!176.58$ & $30.53\!\pm\!17.55$ & $73.42\!\pm\!17.08$ \\
Vendor SDK ($M\!\pm\!SD$) & $972.89\!\pm\!243.36$ & $49.74\!\pm\!18.29$ & $34.47\!\pm\!13.91$ \\
95\% CI (UXM $-$ SDK) & $[-493.25,\,-324.48]$ & $[-24.87,\,-13.55]$ & $[28.69,\,49.21]$ \\
$t(df)$ & $-10.18\,(18)$ & $-7.13\,(18)$ & $7.98\,(18)$ \\
$p$ & $<.0001$ & $<.0001$ & $<.0001$ \\
Hedges' $g_{\mathrm{av}}$ & $-1.84$ & $-1.03$ & $+2.39$ \\
\bottomrule
\multicolumn{4}{@{}p{\textwidth}@{}}{Statistics are computed on paired differences from the supplied raw CSVs. The study supports setup burden in this prepared task; it does not establish general developer productivity or long-term maintainability.}
\end{tabularx}
\end{table}

Table~\ref{tab:user-results} shows lower completion time and lower workload for the UnifiedXRMotion condition, with higher SUS scores. The completion-time result had an order-sensitive pattern across counterbalanced orders (mean difference $-307.6$~s for AB and $-521.4$~s for BA; Welch test $t(16.94)=3.32$, $p=.0040$). Sequence sensitivity was not observed for NASA--TLX ($t(16.10)=-0.31$, $p=.763$) or SUS ($t(16.99)=1.53$, $p=.145$). The setup-burden claim is therefore useful but narrow: it supports the tested procedural configuration task, not broad claims about developer productivity.

\section{Discussion and Limitations}\label{sec:discussion-limitations}
UnifiedXRMotion is evaluated as an implemented contract boundary, not as a standardized interchange format or an end-to-end performance system. The core record does not encode provenance, continuous confidence, schema version, or full timestamp propagation. Provenance is implicit in the producing node, so a downstream consumer cannot tell from the data record alone whether a component was directly observed, inferred, replayed, reconstructed, or retargeted. Vision confidence is collapsed into binary availability in the current adapters, losing information that future versions should preserve.

The adapter portfolio is selected rather than exhaustive. Each new tracker still requires a schema mapping, coordinate conversion, and validation, and some mappings contain source-specific assumptions such as offsets or calibration conventions. The 77-slot schema is one prototype instantiation, not a general avatar skeleton. Arbitrary avatar rigs require schema agreement and retargeting between the runtime representation and the avatar-specific skeleton. The included IK, retargeting, filtering, and codebook-style modules are representative nodes, not algorithmic contributions.

The setup study also has limited scope. Hand-axis alignment and mesh-specific retargeting were pre-completed, participants worked on a prepared Unity task, and the study did not measure adapter-authoring effort, long-term maintenance, or quality of produced motion. Optional local/remote traces and transport utilities characterize application-level runtime behavior only; they should not be interpreted as display-response timing, remote-execution performance benefit, or user-perceived responsiveness.

\section{Conclusion}\label{sec:conclusion}
UnifiedXRMotion reframes heterogeneous XR avatar-motion integration as a validity-aware runtime contract problem. Producers publish observed, inferred, replayed, synthetic, or retargeted components into a shared skeletal representation, and consumers read only explicitly available components through schema views. The Unity prototype demonstrates the contract with selected producer families, adapter mappings, reusable retargeting/recording/transport consumers, and a controlled setup-burden study. The evidence supports a scoped EuroXR medium-paper contribution: a concrete contract and adapter boundary for composing heterogeneous XR avatar-motion pipelines without making downstream consumers depend on SDK-specific motion records.

\bibliographystyle{splncs04}
\bibliography{bib/references}
\end{document}
